# checker
A simple update checker for Arch Linux.

It's really simply made.
# installation
```
git clone https://github.com/hyperfault/checker.git
cd checker
chmod +x checker.sh
sudo mv checker.sh /usr/local/bin/checker
```
Now it can be run from anywhere
```
checker
```
